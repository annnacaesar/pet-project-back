const userSchema = require('./userSchema');
const petSchema = require('./petSchema');
const recoverPasswordSchema = require('./recoverPasswordSchema');

module.exports = {
  userSchema,
  petSchema,
  recoverPasswordSchema
};
